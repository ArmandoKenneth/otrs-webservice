package com.armando.otrs.dao;

import java.util.List;

import com.armando.otrs.model.Sla;

/**
 * Class that contains all the methods that are used to access the sla table in the database
 * 
 * @author Armando
 *
 */
public class SlaDao extends BaseDao{

//	public Sla getSla(int id) throws Exception {
//		try {
//			getAvailabeConnection();
//			String sql = "SELECT * FROM sla WHERE id=?";
//			setPs(getCon().prepareStatement(sql));
//			getPs().setInt(1, id);
//			setRs(getPs().executeQuery());
//			List<Sla> retorno = rsSla(getRs());
//		} catch (Exception e) {
//			throw e;
//		} finally {
//			closeConnection();
//		}
//	}
	
	
	
}
